# CLAUDE.md

## 项目概述

这是一个技能仓库，用于开发技能。每个技能是一个独立目录，包含 `SKILL.md` 文件。

## 目录规范

- 技能放在 `<skill-name>/SKILL.md`，如 `gcf/SKILL.md`
- SKILL.md 使用 YAML frontmatter：`name`（技能名）、`description`（触发条件和使用说明）、可选的 `argument-hint`
